interface:
  display_name: "Quant Platform Radar"
  short_description: "Track Chinese quant platform updates."
  default_prompt: "Use $quant-platform-radar to update JoinQuant, Guorn, and ThinkTrader signals and map them to A-share themes."
policy:
  allow_implicit_invocation: true
